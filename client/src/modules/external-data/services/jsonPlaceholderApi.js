export {
  fetchExternalUsers,
  mapExternalUser,
} from '../../../shared/api/externalDataApi';

export function resetJsonPlaceholderClient() {
  /* Kept for test compatibility; fetch client is stateless after refactor. */
}
