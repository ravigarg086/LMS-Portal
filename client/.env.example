# Optional: direct API URL when not using the CRA dev proxy (e.g. production builds)
# REACT_APP_API_URL=http://localhost:5000/api

# Disable source maps in development builds
GENERATE_SOURCEMAP=false
